# Hono Stripe Integration Example With Tests

This is a simple backend server built with a lightweight framework `Hono`, with Stripe integration and tests.

## Getting started

```
npm install
npm run dev
```

```
open http://localhost:3000
```
